export interface APIInfo {
  Id?: number;
  Name: string;
  DisplayName?: string;
  Description?: string;
  Enabled: boolean;
  // Url: string;
  // Scopes: string;
  // RequireConsent: boolean;
  // RequireSecret: boolean;
}
